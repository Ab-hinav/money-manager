apiVersion: apps/v1
kind: Deployment
metadata:
  name: money-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: money-frontend
  template:
    metadata:
      labels:
        app: money-frontend
    spec:
      containers:
      - name: frontend
        image: abhi7dev/money-manager-frontend:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
        env:
        - name: NEXT_PUBLIC_API_URL
          value: "http://money-backend-service:80"
        - name: NEXTAUTH_SECRET
          value: "your-super-secret-random-token-change-this-in-prod"
        - name: NEXTAUTH_URL
          value: "http://13.234.232.184" # Update this if using a domain
---
apiVersion: v1
kind: Service
metadata:
  name: money-frontend-service
spec:
  selector:
    app: money-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000